{
  "name": "@sparta/scheduler",
  "version": "0.1.0",
  "type": "commonjs",
  "main": "src/lambda.js",
  "scripts": {
    "dev": "bun run src/local-invoke.js",
    "test": "SKIP_MSG=true LOG_LEVEL=silent bun run src/validator-monitor.test.js > /dev/null 2>&1",
    "test-verbose": "SKIP_MSG=true bun run src/validator-monitor.test.js",
    "build": "rm -rf dist && esbuild ./src/*.js --entry-names=[dir]/[name]/index --bundle --minify --sourcemap --platform=node --target=node16.14 --outdir=dist &&  zip -r lambda-package.zip dist"
  },
  "dependencies": {
    "@sparta/utils": "workspace:*",
    "@sparta/ethereum": "workspace:*",
    "@sparta/discord": "workspace:*",
    "openapi-client-axios": "^7.5.1"
  },
  "devDependencies": {
    "aws-sdk": "^2.1499.0"
  }
} 